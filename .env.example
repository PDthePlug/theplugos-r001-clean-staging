# Staging browser configuration. Leave blank only for landing/local-shell work.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional for constrained preview environments.
# DISABLE_HMR=true

# Never put service-role keys, access tokens, or database passwords in VITE_*.
